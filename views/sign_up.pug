extends layout
include mixins/_user_form 

block content
    .signupPage
        h2= title
        if errors
            ul
                for error in errors
                    li= error.msg
        hr
        +userForm()